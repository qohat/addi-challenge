Close out spec 04 first, as usual.
Then three bits of maintenance before we continue:
- One. run-spec still has the gap I flagged: it never says to write the next spec, so I've run out of written specs twice now. Add a step between "Hand it over" and "Export" — once the PR is merged, write the next spec in the queue with create-spec, from the code as it now is, committed straight to main. And fix the stop condition: "about to touch a second spec" currently reads as if writing the next spec were forbidden. Implementing two specs in one session is what's banned; specifying the next one is how a session is supposed to end.
- Two. Add a Priority column to the queue — core or nice-to-have — plus a short Scope note saying what gets dropped first if I run out of time. My read: 04, 05 and 06 are core, they're the remaining business rules; 08 is core because the README is an explicit requirement in section 5 of the brief; 07 is the only nice-to-have, since a demo command is reviewer convenience. Say plainly that dropping 07 still leaves a coherent system.
- Three. Add a projection to docs/ai/cost.md, not the queue — it's write-only data. My medium specs have cost $2.20–$3.41 and the one high cost $6.22, so what remains projects to roughly $16 and the project to about $42 total.
Then write spec 05 with create-spec. Only that one. It's marked low, and I'm going to run it on Sonnet in the next session as a real test of whether the spec is good enough — so it has to be genuinely mechanical. Every shape decided, every test named, nothing left to invent. If you can't get it there, tell me and mark it medium instead of pretending.